package org.santayn.reservation.models.schedule;

/**
 * Тип недели для расписания: чётная, нечётная или любая (обычная).
 */
public enum WeekParityType {
    EVEN,   // чётная
    ODD,    // нечётная
    ANY     // обычная (без различия чётности)
}
